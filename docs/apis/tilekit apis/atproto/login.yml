info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{tilekit_base_url}}/atproto/login"
  body:
    type: json
    data: |-
      {
        "user_handle": "madcla.ws"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
