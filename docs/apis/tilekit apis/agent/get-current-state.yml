info:
  name: get-current-state
  type: http
  seq: 3

http:
  method: GET
  url: "{{tilekit_base_url}}/agent/state"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Starts the Pi agent
