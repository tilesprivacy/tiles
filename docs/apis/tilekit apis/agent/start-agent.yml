info:
  name: start-agent
  type: http
  seq: 1

http:
  method: GET
  url: "{{tilekit_base_url}}/agent/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Starts the Pi agent
