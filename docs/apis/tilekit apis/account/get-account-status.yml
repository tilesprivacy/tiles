info:
  name: get-account-status
  type: http
  seq: 2

http:
  method: GET
  url: "{{tilekit_base_url}}/account/status"
  body:
    type: json
    data: |-
      {
        "nickname": 'yo'
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
