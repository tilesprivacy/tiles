info:
  name: save-chat
  type: http
  seq: 4

http:
  method: POST
  url: "{{tilekit_base_url}}/session/chat"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
