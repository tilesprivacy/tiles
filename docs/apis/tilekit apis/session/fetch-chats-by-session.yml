info:
  name: fetch-chats-by-session
  type: http
  seq: 3

http:
  method: GET
  url: "{{tilekit_base_url}}/session/{session_id}/chats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
