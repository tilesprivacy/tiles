info:
  name: list-sessions
  type: http
  seq: 2

http:
  method: GET
  url: "{{tilekit_base_url}}/session/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
