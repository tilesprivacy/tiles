info:
  name: stop-server
  type: http
  seq: 3

http:
  method: GET
  url: "{{tilekit_base_url}}/server/stop"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
