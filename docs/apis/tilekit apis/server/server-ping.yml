info:
  name: server-ping
  type: http
  seq: 2

http:
  method: GET
  url: "{{tilekit_base_url}}/server/ping"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
