info:
  name: get-config
  type: http
  seq: 2

http:
  method: GET
  url: "{{tiles_base_url}}/config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
