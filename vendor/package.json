{
  "name": "tiles-vendor",
  "version": "1.0.0",
  "private": true,
  "description": "Vendored Pi extensions for Tiles. Update with: npm install --package-lock-only --legacy-peer-deps && ./refresh.sh",
  "dependencies": {
    "pi-mcp-adapter": "2.28.0"
  }
}
