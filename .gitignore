/target
.tiles_dev
dist/
stack_export_prod/
*.profraw
tilekit/target
tiles/target
.DS_Store
pkgroot/
pkgroot_gguf/
downloads/
*.pkg
models/
pkgroot_models/
pi-darwin-arm64.tar.gz
pi-linux-x64.tar.gz
.cursor/hooks/state/continual-learning.json
.cursor/hooks/state/continual-learning-index.json
.cache
server/bin
server/stack/linux/requirements/
downloads
server/stack/macos/requirements/

# menu bar app
node_modules/
apps/menubar/src-tauri/gen/

# the vendored adapter is committed on purpose, so it must survive both the
# node_modules/ rule above and the dist/ rule at the top: git cannot re-include
# below an excluded directory, so the directory itself is unexcluded first
!vendor/node_modules/
!vendor/node_modules/**

# chat UI checkout, fetched at the pin in apps/menubar/ui.pin
apps/menubar/.ui-src/

pkg/component-app.plist
pkg/tiles-app-unsigned.pkg
